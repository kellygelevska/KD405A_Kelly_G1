
public class bike {

	private String color;
	private int price;
	private int size;

	//Constructors


	public bike(String color, int size){
		
		this.color = color;
		this.size = size;


		}

	public bike(String color, int size, int price){
		this.color = color;
		this.price = price;
		this.size = size;
	}

		/*for (int i = 0; i < 10; i++) {
			if (0 == color.compareTo(constants.bikeColors[i].toString())) {
				this.color = color;
				break;
			}else{
				this.color = "ERROR"; }
	
	}
	if (size >= 8 && size <= constants.MAX_SIZE) {
		this.size = size;
		
	}else{
		this.size= 500;
		
		
	}
	if (price >= 0 && price <= constants.MAX_PRICE) {
		this.price = price;
	}else{
		this.price= 500;
	}*/

	//methods 

	public String getColor () {
		return this.color; 
	}

	public int getSize () {
		return this.size;
	}
	
	public int getPrice () {
		return this.price;
	}
	
	public void setPrice (int price) {
		if (price >=0 && price <= MAX_PRICE) {
			this.price = price; 
		}
	}
}


